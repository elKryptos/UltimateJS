// antes de escribir la expresion se tiene que tabular
// si la expresion va a contener muchas lineas de codigo
// se tiene que usar las llaves

let edad = 18;
if (edad >= 18) {
    console.log('Usuario mayor de edad');
} else if(edad>13) {
    console.log('Usuario necesita ser acompaniado de sus padres');
} else {
    console.log('Usuario menor de edad');
}